import Layout from '#/components/Layout';
import LoginPage from '#/components/LoginPage';

const IndexPage = (): React.ReactElement => (
  <Layout title="Canto da Rua - Login Teste DEPLOY">
    <LoginPage />
  </Layout>
);

export default IndexPage;
